@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@{
    Layout = "~/Views/Front_Layout.cshtml";
}
<section>
    <form>
        <div>

            <a class="btn btn-success" asp-action="SRoomreservation" role="button">checkavalabilty of surgery</a>
        </div>

        <hr />
    </form>
</section>
<section class="container">
    <form asp-action="Event">
        <h2>Rooms reservation</h2>
        <hr />
        <div class="row">

            @foreach (var fp in ViewBag.h)
            {
                <div class="card col-md-3 mx-2 mb-3 p-0">
                    <div class="card-body">
                        <h4 class="card-title text-danger"> @fp.Type Reservation </h4>
                        <button name="Room_Id" value="@fp.Type" type="submit" class="stretched-link"></button>
                    </div>
                </div>
            }
        </div>
        <hr />
        <h2>Surgery Rooms reservation</h2>
        <div class="row">

            @foreach (var fp in ViewBag.h1)
            {
                <div class="card col-md-3 mx-2 mb-3 p-0">
                    <div class="card-body">
                        <h4 class="card-title text-danger"> @fp.Type Reservation </h4>
                        <button name="Room_Id" value="@fp.Type" type="submit" class="stretched-link"></button>
                    </div>
                </div>
            }
        </div>
    </form>
</section>